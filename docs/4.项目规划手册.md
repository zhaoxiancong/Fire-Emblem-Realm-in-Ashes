# 《山河烬》项目规划手册

> **文档定位**：本项目的**单一规划来源（Planning SSOT）**。所有关于"现在做什么、下一步做什么、做到什么程度算完成"的问题，以本手册为准。
>
> **与其它文档的分工**：
> - **做什么系统**（设计意图）见 `2.设计文档.md`
> - **所有数字**见 `3.游戏数值设定.md`（唯一来源）
> - **讲什么故事**见 `1.剧情与世界观设定.md`
> - **怎么操作**（环境/构建/迁移/发布）见 `5.改版工程手册.md`
> - **方案 B 的实施细节**见 `6.方案B内容外置规划.md`
> - **框架怎么运作**见 `7.框架架构与机制详解.md`
> - **资产怎么接**见 `8.原创资产体系与框架资产对照.md`；**角色槽位怎么排**见 `9.角色槽位映射与美术平替方案.md`
> - **关卡数据怎么产出**见 `10.关卡表数据化方案.md`
> - **本手册只管规划**：阶段、依赖、门禁、风险、进度。

**文档版本**：v1.11
**编写日期**：2026-09-25
**基线**：框架 commit `ec1dc855`；中英文双 ROM 构建通过；中文渲染实机验证通过

---

## 0. 为什么需要这份手册

### 0.1 问题：规划已经漂移

截至 2026-09-25，项目里**并存着两套互不引用的路线图**：

| 出处 | 形态 | 写作时间 | 状态 |
|---|---|---|---|
| `2.设计文档.md` §7 | **时间轴式**：P0 第 1~2 周 → P1 第 2~6 周 → P2 第 2~4 月 → P3 第 5 月 → P4 第 6 月起 → P5 发布 | v0.6 时期 | **已过时**（P0 已完成，且方案 B 拍板后架构变了） |
| `6.方案B内容外置规划.md` §4 | **验收式**：Phase 1 骨架与验证 → 2 第一个内容 → 3 机制外置 → 4 规模化 → 5 资产管线 | 方案 B 后 | **较新**，但只覆盖工程与内容，未涵盖数值/关卡/发布 |

同时，待办事项**散落在五个文档**里：

| 文档 | 待办位置 |
|---|---|
| `2.设计文档.md` | §9 内容侧进度与待办 |
| `3.游戏数值设定.md` | §13 数值待规划清单（P0~P3 共 10 项） |
| `5.改版工程手册.md` | §8 待办（6 项） |
| `6.方案B内容外置规划.md` | §4 路线图 + §7 立即要做的三件事 |
| `7/8/9` | 各自的"风险"与"待决"章节 |

**后果**：想知道"现在该做什么"，要翻五个文档、自己拼装、还得判断哪个是旧的。**这是项目最大的隐性成本。**

### 0.2 解法：本手册作为单一规划来源

1. **本手册是唯一的"进度看板"**——所有阶段的定义、状态、门禁都在这里。
2. **其它文档的路线图章节一律降级为指针**（指向本手册），不再各自维护。
3. **任务细节仍然写在各专业文档**：数值 TODO 在文档 3、工程操作在文档 5、资产格式在文档 8。本手册**只做索引与节奏控制**，不复制它们的内容。
4. **每次会话收尾**：如果推进了某个任务，回本手册更新状态（见 §9）。

### 0.3 怎么用这份手册

| 你的问题 | 看哪一节 |
|---|---|
| 下一步该干什么？ | §4 里程碑，找第一个未完成的 M |
| 这个阶段做完没有？ | §5 门禁判定规则（有可检验的判据） |
| 为什么现在不能做 X？ | §6 依赖关系与关键路径 |
| 有什么坑？ | §7 风险登记册 |
| 需求变了怎么办？ | §8 变更与决策管理 |
| 有什么还没拍板？ | §10 待拍板决策总表 |

---

## 1. 项目定义

### 1.1 一句话目标

> **做出一款基于 FE8 反汇编扩展框架的中国风原创战棋 SRPG《山河烬》，20 章单线、22 名可玩角色，画风 100% 继承原版 GBA 火纹，以补丁形式免费分享。**

### 1.2 完成定义（Definition of Done）

项目**整体**完成的判据（缺一不可）：

| # | 判据 | 可检验方式 |
|---|---|---|
| 1 | 20 章主线全部可通关，无阻断性 bug | 完整流程实机走通 |
| 2 | 22 名角色全部可用（数值/立绘/动画/支援齐备） | 角色清单逐项核对 |
| 3 | 中文文本全量覆盖，无豆腐块、无溢出 | 逐章实机目视 |
| 4 | 神器系统、双环克制、凶相机制全部落地并验证 | 回归场景全通过 |
| 5 | 从**全新克隆**本仓库跑一条命令，能重建出与发布版一致的 ROM | 干净环境复现测试 |
| 6 | 产出 `.ups`/`.bps` 补丁包 + 打补丁说明 | 分发流程走通一次 |

### 1.3 边界（明确不做）

| 不做 | 理由 |
|---|---|
| 不发布原版 ROM（只发补丁） | 法律 |
| 不做商业化、不上架 | 用户前提① |
| 不做原生 Windows 构建 | 框架不支持，走 WSL2 |
| 不做手机端适配 | 模拟器统一承载 |
| 不重绘全部美术 | 复用原版资产是选型初衷（见 `0.调研报告.md` §11.2） |
| 不做联机 / 成就 / 云存档 | 超出范围 |

### 1.4 已拍板决策索引

**不在此处复制**（避免第二份真相）。完整决策表见 `2.设计文档.md` §1（24 项），其中与规划强相关的关键决策：

- 技术路线 = FE8 反汇编扩展框架（`0.调研报告.md` §11）
- 版本管理 = **方案 B 内容外置**（`2.设计文档.md` §12.6 → `6.方案B内容外置规划.md`）
- 中文字体 = 框架官方 CJK 方案，**必须 32M ROM**
- 体量 = 20 章单线 + 外传，可玩 22 人，职业 15 链
- Demo = 序章 ~ 第 4 章（5 关）

### 1.5 成功判据（阶段性）

| 级别 | 判据 |
|---|---|
| **可运行** | ROM 能构建、能启动、能进战斗 |
| **可游玩** | Demo 五章能从头打到尾 |
| **可交付** | 全新克隆可复现 + 补丁可分发 |
| **完整** | §1.2 的六条全部满足 |

---

## 2. 当前基线（as of 2026-09-25）

### 2.1 已完成（有证据）

| # | 成果 | 证据 |
|---|---|---|
| 1 | WSL2 + Ubuntu-24.04 环境打通（镜像网络 + 代理 + 清华源） | `5.改版工程手册.md` §2 |
| 2 | 框架克隆并构建成功（宿主机工具 + 子模块问题已解决） | `6.方案B内容外置规划.md`；`7.框架架构与机制详解.md` §4 |
| 3 | **16M 英文 ROM** 构建 + boot-check 通过 | SHA1 `8d16fa64…`，`FIREEMBLEM2E / BE8E` |
| 4 | **32M 中文 ROM** 构建 + boot-check 通过 | SHA1 `508f6109…`，已拷 `D:\workbuddy\shanhe-rom\` |
| 5 | **中文字库容量实测 → 风险撤销** | 字形 3,842 / 字库 258.9 KB / ROM 空闲 ~14.66 MB |
| 6 | **中文渲染实机验证通过（P0 关闭）** | mGBA 目视：`报、报告！` 清晰、全角标点正确、无豆腐块 |
| 7 | 技术路线拍板（路线 A+）+ 方案 B 拍板（内容外置） | `0.调研报告.md` §11；`2.设计文档.md` §12.6 |
| 8 | 设计资产齐备 | 20 章大纲、22 角色、15 职业链、Demo 五章剧情、数值基础（Demo 七人七职业） |
| 9 | 规划/参考文档体系建立 | 文档 `6/7/8/9` 完成（方案 B、框架详解、资产体系、槽位映射） |
| 10 | 文档防篡改机制建立 | `tools/git-health.sh` + `GIT_WORKFLOW.md` §5/§6 |

### 2.2 进行中

| # | 事项 | 状态 |
|---|---|---|
| 1 | 本手册（`4.项目规划手册.md`） | v1.0 编写中 |

### 2.3 未开始（全部待办，按 §3 工作流归类）

**S1 工程基建**：建 `content/` 结构 + `framework.lock`；写 `tools/shanhe-build.sh`；更新 `.gitignore`；端到端验证；可恢复性验证；全新克隆重现测试

**S2 内容管线**：虞聪入表（角色+职业+初始武器）；中文化菜单文本；3,414 条原版消息中译导入；支援 13 组

**S3 机制实现**：双环克制（兵刃三环 + 三才法环）；神器系统；凶相机制；ID 空间扩展；**惊雷引特效打通全链路（M3）**

**S4 关卡制作**：序章 ~ 第 4 章全部关卡（地图/单位/商店/陷阱/事件脚本）；第 5 章起逐章

**S5 资产管线**：**★ 地图瓦片集攻坚（M3 前置，D-3 定案）**；立绘（22 套，M4 先做 7 套）；调色板派生；章节地图（20 章）；音乐注册；职业图标；战斗动画（主角 1 套）；神器特效

**S6 数值与平衡**：剩余 14 名角色五维；15 链全阶；各章商店/宝箱；BOSS/凶相数值；Demo 五章敌军配置；playtest 调参

**S7 验证与发布**：回归基线（三环/神器/中文渲染三组）；补丁打包流程；分发

### 2.4 已知阻塞与前置决策

| # | 阻塞项 | 阻塞什么 | 状态 |
|---|---|---|---|
| 1 | ~~**22 个原版角色槽位分配**~~ | 立绘、动画、支援、剧情 | ✅ **已定案（2026-09-25）**，见 `9.角色槽位映射与美术平替方案.md` §3.2 / §3.4 |
| 2 | ~~**9 件神器特效精简方案**~~ | 神器视觉表现 | ✅ **已定案（2026-09-25）**：9 件只需 3 个 ID，见文档 8 §7.2 |
| 3 | ~~**美术预算重心**~~ | 整体工期 | ✅ **已定案（2026-09-25）**：分期摊销（瓦片集前置），见文档 8 §14 |
| 4 | **体量红线：20 章 vs 12~15 章** | 全量阶段规模 | 未定（M5 决策门裁定） |
| 5 | ~~**悔棋系统是否保留**~~ | 机制层工作量 | ✅ **已定案（2026-09-25）：不做**。依赖模拟器即时存档 + 原版中断存档兜底，见文档 2 §2 |
| 6 | ~~**关卡表文档缺失**~~ | 关卡制作的输入 | ✅ **已定案（2026-09-25）：不建文档，直接数据化**。设计意图留文档 1 §4.6，数据落 `content/data/chN_*.json`（每章一个 bundle），见 `10.关卡表数据化方案.md` |
| 7 | ~~**M3 是否新增瓦片集攻坚任务**~~（D-3 带出） | 20 章地图前提 | ✅ **已定案（2026-09-25）：新增**，作为 M3 交付物 ⑤ 与验收标准 6，见文档 8 §14.5 |
| 8 | ~~**M4 验收标准是否修订**~~（D-3 带出） | M5 决策门数据质量 | ✅ **已定案（2026-09-25）：修订**，新增软指标 6/7（7 套 Demo 立绘 + 单套/单章工时记录），见文档 8 §14.3 |

> 第 1~3、5~8 项已全部定案。第 7、8 项为 D-3 带出的新待确认项，**已于 2026-09-25 按建议定案**；第 6 项（D-6）**已于 2026-09-25 定案**→ 不建文档、直接数据化，见 `10.关卡表数据化方案.md`；第 5 项（D-5）**已于 2026-09-25 定案**→ 悔棋不做，见文档 2 §2。**§2.4 阻塞项已全部关闭；唯一剩余待拍板是 D-4（体量红线，M5 决策门裁定，非可立即决定项）。**

---

## 3. 工作分解结构（WBS）

本项目的任务**不是一条直线**，而是**七条并行工作流**（stream）。里程碑（§4）是"某几条流同时达到某个状态"的横切面。

| 流 | 名称 | 内容 | 主要产出位置 |
|---|---|---|---|
| **S1** | 工程基建 | 方案 B 的骨架：`content/` 结构、同步脚本、可恢复性 | 本仓库根目录 + `tools/` |
| **S2** | 内容管线 | 数据表（角色/职业/道具/支援）+ 中文文本 | `content/data/`、`content/texts/` |
| **S3** | 机制实现 | 原创 C 代码：双环克制、神器、凶相 | `content/src/shanhe_*.c` |
| **S4** | 关卡制作 | 每章的地图/单位/商店/陷阱/事件脚本 | `content/data/chN_*.json`、`content/src/events/` |
| **S5** | 资产管线 | 立绘/动画/地图/音乐/图标/特效 | `content/assets/` |
| **S6** | 数值与平衡 | 所有数字（唯一定义在文档 3） | `3.游戏数值设定.md` → `content/data/` |
| **S7** | 验证与发布 | 回归基线、构建验证、补丁打包 | `5.改版工程手册.md` §5/§6 |

**设计意图**：用"工作流"而非"阶段"切分，是为了**可扩展**——将来新增任何任务，都能归入某一流；也便于**并行**（如 S5 资产可与 S2/S3/S4 并行，因为有槽位映射表兜底）。

---

## 4. 里程碑与门禁

**约定**：每个里程碑给出「目标 / 涉及工作流 / 交付物 / 验收标准 / 依赖」。**验收标准一律可检验**（能跑命令、能目视、能数数）。

---

### M0 · 基线与选型 ✅ **已完成**

| 项 | 内容 |
|---|---|
| 目标 | 环境跑通、技术路线拍板、P0 风险关闭 |
| 工作流 | S1（部分） |
| 交付物 | 16M/32M 双 ROM；字库实测报告；路线决策文档 |
| 验收 | ✅ 双 ROM boot-check 通过；✅ 中文实机渲染无豆腐块；✅ 决策表落档 |
| 依赖 | 无 |

---

### M1 · 骨架可重建 ✅ **已完成（2026-09-25）**

| 项 | 内容 |
|---|---|
| 目标 | 建立方案 B 的全部骨架，做到"**全新克隆 + 一条命令 → 可玩 ROM**" |
| 工作流 | S1（全部）+ S7（部分） |
| 交付物 | ① `content/{data,texts,src,assets}` 目录结构<br>② `framework.lock`（钉 `ec1dc855` + 子模块 + 基线 SHA1）<br>③ `tools/shanhe-build.sh`（六步 + `DRY_RUN`/`STATUS`/`RESTORE`/`SKIP_BUILD`）<br>④ `.gitignore` 更新<br>⑤ 验证记录（`11.M1骨架验证记录.md`） |
| 验收标准（**六条全过才算完成**） | 1. `DRY_RUN=1` 预演不落盘、输出可读 ✅<br>2. 实跑一次，产出的 32M 中文 ROM **产物正确**——文件存在、33554432 字节、header `FIREEMBLEM2E`/`BE8E`、mGBA 实机可进入游戏并目视到中文 ✅（零改动基线 SHA1 `508f6109` 与已验证基线逐字节一致）<br>3. `RESTORE=1` 后框架**逐字节还原**到 `ec1dc855`（SHA1 比对）✅<br>4. `STATUS=1` 能列出框架侧全部改动 ✅<br>5. 在另一目录 `git clone` 本仓库 → 跑一条命令 → 得到可玩 ROM ✅<br>6. 反查步骤能抓到"预期外改动" ✅ |
| 依赖 | M0 |
| 风险 | 同步脚本的合并逻辑（见 §7 R-01/R-02）；**验收工具的选择（见下）** |

> **⚠️ 关于第 2 条为何不提 `boot-check`（2026-09-25 审计修正）**：框架官方的 `expansion-modern-boot-check` 用 `--policy behavior` 比对，而该策略**只排除顶层 `rom` 键**，其余（含逐帧像素哈希 `framebuffer_hash`）**全部严格比对**。一旦启用中文，title 画面像素必然改变 → 该目标**必然失败**。它是上游对自身基线的回归门禁，**不是改版项目的验收工具**。故第 2 条改为**自建的产物校验**（尺寸 + header + 实机可引导 + 目视），详见文档 6 §3.1 第 5 步。

**⚠️ 本里程碑的硬约束**：**未通过前不要开始写游戏内容**——否则内容散落框架仓库，又回到需要迁移的状态。

**★ M1 完成时的关键发现（交接给 M2）**：实施中实测出 **round-trip 约束**——框架 16 张 generated-data 表分两组，5 张无 round-trip（章节/UI 表，可自由改），11 张有 round-trip（改 JSON 必须同步改手写 C，否则 `make` 硬失败）。`items` 有官方 `ITEM_EXPANSION_*` 豁免通道，**`characters`/`classes`/`supports` 没有**。→ **已专项调研完成，定案 B'（生成产物回填）**：`generate --no-roundtrip` 的产物覆盖 `src/data_characters.c`，实测 `make` 通过、ROM 与手写改法逐字节相同。详见文档 6 §3.4a/§3.4b。

**可并行推进**（不依赖 M1，见 §2.4）：**已无待决项** —— D-1/D-2/D-3/D-5/D-6 五项已于 2026-09-25 全部定案；D-4（体量红线）是 **M5 决策门**裁定项，**不可提前决定**（须等 M4 的工时数据）。

---

### M2 · 内容管线打通 ← **当前目标**

| 项 | 内容 |
|---|---|
| 目标 | 证明"改数据 → 进游戏"这条链路端到端可用 |
| 工作流 | S2 + S1（验证） |
| 交付物 | ① 虞聪（角色 + 职业 + 初始武器）三表落位<br>② 一批菜单文本中文化<br>③ 「加内容的标准流程」写进文档 5 |
| 验收标准 | 1. 虞聪在游戏内**名字、数值、职业正确显示**<br>2. `validate / generate / check` 三动词全过<br>3. 新增 `.c`/`.json` 后 ROM 内含对应符号（map 反查）<br>4. 实机目视确认 |
| 依赖 | M1 |
| 注意 | **① 新增 `.c` 后必须先 `touch Makefile`**（见文档 7 §4.2）；**② 原创角色入表走 B' 方案**（见文档 6 §3.4b）——**✅ 已在 `shanhe-build.sh` 3a' 实现**（三道：数据校验 → B' 回填 → round-trip 复核），脚本自动完成；**③ 中文文本有两层通道**（见文档 6 §3.4d/§3.4e）——`indexed_overrides`（3b'）只做审计**不进 ROM**，**真正进 ROM 的是 `msg_overrides`（3b''）**，两者都要写；**④ 原创汉字要补字库**（见文档 6 §3.4f）——字形不走文本通道，走 `content/fonts/shanhe-font-patch.tar.gz`（3c'） |
| 进度 | ✅ **① 角色表**（虞聪数值顶替艾瑞珂槽，ROM 实测通过）<br>✅ **① 职业表**（佩剑郎顶替 `CLASS_EIRIKA_LORD`，M2 首批）<br>✅ **① 姓名/职业文本**（虞聪/佩剑郎，**消息表通道已字节级确证进 ROM**：Huffman 解压 + ROM 压缩字节命中）<br>✅ **① 汉字字形**（佩拗耳聪虞郎鸣鼎 8 字入字库，字库符号实测 +8/+3）<br>⬜ **① 初始武器**（照夜，待 M3 神器机制一起做）<br>⬜ ② 菜单文本中文化（通道已通，待批量填）<br>⬜ ③ 标准流程写进文档 5（✅ 已写 §3.7/§4.4/§5.4） |

---

### M3 · 机制外置（第一个 C 扩展）+ 美术前置攻坚

| 项 | 内容 |
|---|---|
| 目标 | 打通"原创 C 代码进 ROM"的链路，落地第一个战斗机制；**并提前攻克地图瓦片集壁垒** |
| 工作流 | S3 + S1 + S6 + **S5（瓦片集攻坚，D-3 定案新增）** |
| 交付物 | ① 双环克制（`weapontriangle.json`，可能零 C 代码）<br>② 至少一件神器的加成机制<br>③ ID 空间扩展落地（`--with-item-id-cap`）<br>④ **「惊雷引」1 个自定义法术特效打通全链路**（验证 D-2 路径，见文档 8 §7.2）<br>⑤ **★ 地图瓦片集攻坚**：产出「Demo 五章原版瓦片集复用清单」+「必须新增的元瓦片清单」+ 一条实机验证（自选/自建瓦片集接进 `gChapterDataAssetTable` 并正确显示） |
| 验收标准 | 1. 克制修正在**战斗预测面板**正确显示<br>2. 神器加成在实战中数值正确<br>3. ID 空间扩展后编译期静态断言通过<br>4. `HOOKS=0` 时战斗数学与原版字节一致（回归）<br>5. **惊雷引特效在实机播放正确**（帧数 / OAM / ROM 字节三上限内）<br>6. **第 1 章地图能用自选/自建瓦片集在 mGBA 正确显示，且地形通行性正确** |
| 依赖 | M2 |
| 关键风险 | ① **`ExpansionMechanicsRegister()` 的 apply 顺序陷阱**——需要双方克制关系的机制不能挂这个 seam（见文档 7 §6）<br>② **R-16 瓦片集自产能力未验证**——这是 20 章地图的前置壁垒，必须在本里程碑关闭（见文档 8 §14.5） |
| 为什么把瓦片集放在这里 | **D-3 定案的直接落地**：瓦片集是"1 次极高成本 + 不做则 20 章全部无法开工"的一次性壁垒。放在 M3 而非 M4，是为了让它在**任何批量地图绘制之前**暴露真问题。 |

---

### M4 · Demo 五章可通关

| 项 | 内容 |
|---|---|
| 目标 | **序章 ~ 第 4 章完整可玩**——这是项目的第一个真正交付 |
| 工作流 | S2 + S3 + S4 + S5（部分）+ S6 + S7 |
| 交付物 | ① 5 章关卡全套 JSON（含事件脚本）<br>② Demo 7 人数值/立绘/动画映射<br>③ 神器首件（照夜）<br>④ 5 章中文文本<br>⑤ Demo 五章敌军配置表 |
| 验收标准 | **硬门禁（不通过则 M4 未完成）**：<br>1. 序章到第 4 章**连续通关无阻断**<br>2. 战斗预测/危险范围/中断存档**全部正常**<br>3. 7 名角色各有立绘与战斗动画（**可用原版**——Demo 不阻塞于美术）<br>4. 中文文本**无豆腐块、无溢出**<br>5. 走一次完整分发流程（打补丁 → 给自己或一位同好试玩）<br><br>**软指标（D-3 定案新增，为 M5 决策门提供数据）**：<br>6. **完成 7 套 Demo 立绘**（可为调色板派生），并**记录单套实际工时**<br>7. **记录单章地图的实际工时**（5 章样本） |
| 依赖 | M3 |
| 备注 | 「关卡表」缺失问题必须在此前解决（§2.4 第 6 项） |

---

### M5 · 决策门 ⚠️ **项目最重要的一个门**

| 项 | 内容 |
|---|---|
| 目标 | 基于 Demo 的真实产出速度，**裁定项目体量** |
| 判定内容 | ① 走 20 章全量，还是收缩到 12~15 章？（D-4）<br>② 美术预算重心最终分配（依据 M4 软指标 6/7：单套立绘工时 + 单章地图工时）<br>③ 双难度是否保留（D-7，若此时已有结论可一并裁定） |
| 依据 | Demo 五章的实际耗时（人时）+ **单套立绘工时 + 单章地图工时**（M4 软指标产出）+ 剩余工作量估算 |
| 输出 | 一份**书面的体量决策**，写回本手册 §8 |
| 依赖 | M4 |
| **为什么放在这里** | 在 Demo 之前决定体量是**空想**；在 Demo 之后决定才有数据。这是"可落地"的关键设计。 |

---

### M6 · 全量

| 项 | 内容 |
|---|---|
| 目标 | 按 M5 裁定的体量，完成全部章节与角色 |
| 工作流 | 全部 |
| 交付物 | ① 剩余章节逐章制作<br>② 剩余 15 名角色数值/立绘/动画<br>③ 支援 20~40 组<br>④ 原创 BGM 替换<br>⑤ 困难模式 |
| 验收标准 | 全部章节可通关；角色清单逐项齐备；支援对话可达 |
| 依赖 | M5 |

---

### M7 · 发布

| 项 | 内容 |
|---|---|
| 目标 | 产出可分发的补丁包 |
| 工作流 | S7 |
| 交付物 | `.ups`/`.bps` 补丁 + 打补丁说明 + 图文教程 |
| 验收标准 | §1.2 六条全过 |
| 依赖 | M6 |

---

## 5. 门禁（Gate）判定规则

每个里程碑的验收标准是**门禁**。规则：

1. **全部条目为真**才能标记里程碑完成。**不允许"基本完成"**。
2. 某项无法验证时，**视为未通过**（直到可验证）。
3. 未通过时**不进入下一个里程碑**，但**可以并行推进其他工作流**（例如 M1 未过时，可推进 S5 的资产决策与 S6 的数值填充）。
4. 门禁结论记入本手册 §9 的进度表。

---

## 6. 依赖关系与关键路径

### 6.1 关键路径

```
M0 → M1 → M2 → M3 → M4 → M5 → M6 → M7
            ↑
         （当前所在）
```

**M1 是全项目的关键路径起点**（已于 2026-09-25 完成），也是风险最高的一环（它是唯一"没有前人做过"的工程活动——其他环节都有框架文档与范例兜底）。

### 6.2 可并行的工作流

| 可并行的 | 与什么并行 | 为什么可行 |
|---|---|---|
| S5 资产决策（槽位/特效/预算） | M1 | 纯创作决策，不碰代码 |
| S6 数值填充（剩余 14 人 + 15 链） | M1、M2 | 只改文档 3，不碰工程 |
| S7 回归基线的**设计**（不实现） | M1 | 先想清楚要测什么 |
| S2 中文文本的**翻译准备** | M1、M2 | 文本可在 `content/texts/` 里先写 |

### 6.3 硬依赖（不可违反）

| 约束 | 理由 |
|---|---|
| S4 关卡制作 **必须** 在 M1 之后 | 否则内容散落在框架仓库 |
| S3 机制 **必须** 在 M2 之后 | 需要先证明数据管线可用 |
| M5 决策 **必须** 在 M4 之后 | 需要真实速度数据 |
| 22 槽位分配 **必须** 在 S5 立绘批量制作之前 | 立绘 ID 是硬约束（172 槽已满） |

---

## 7. 风险登记册

**统一编号管理**。各专业文档的风险细节仍在原处，此处做**索引 + 等级裁定**。

| # | 风险 | 等级 | 影响 | 对策 | 详见 |
|---|---|---|---|---|---|
| **R-01** | 同步脚本合并逻辑写错，导致内容丢失 | 🔴 高 | 数据损坏 | 增量合并（非覆盖）+ `DRY_RUN` + 写前 SHA1 快照 + `RESTORE` 回滚 | 文档 6 §3.4/§3.3 |
| **R-02** | 新增 `.c` 后未 `touch Makefile` → "文件在、编译过、ROM 里没有" | 🔴 高 | **静默失败** | 同步脚本内建 `touch Makefile`；map 反查验证符号 | 文档 7 §4.2 |
| **R-03** | 立绘 ID 仅 172 槽且已满 | 🔴 高 | 角色无法全部上镜 | 替换原版槽位；先定 22 槽分配 | 文档 9 §3 |
| **R-04** | 战斗动画 Manual-only，单位成本最高 | 🔴 高 | 工期 | 大量复用 + 调色板派生；只给主角做原创 | 文档 8 §7.1 |
| **R-05** | 资产"四要素"缺一 → 静默不进游戏 | 🟠 中高 | 静默失败 | 同步脚本必须做 map 反查 | 文档 6 §6.3 |
| **R-06** | GBA 内存预算（EWRAM/IWRAM）紧张 | 🟠 中高 | 系统加不进去 | `make expansion-modern-linker-check` 每次验证 | 文档 2 §8 |
| **R-07** | 原版战斗代码改造引入 bug | 🟠 中高 | 行为错乱 | 建回归基线；每次改动跑自建产物校验 + 战斗数值对拍（**勿用上游 boot-check**，见 M1 注） | 文档 2 §8 |
| **R-08** | 法术特效 ID 仅 16 个 | 🟠 中 | 神器表现受限 | 特效精简或走战斗动画路径 | 文档 8 §7.2 |
| **R-09** | 外部工具篡改 `.md`（含 HTML 转义损坏） | 🟠 中 | 文档损坏 | `tools/git-health.sh` 体检 + `git checkout` 还原 | `GIT_WORKFLOW.md` §6 |
| **R-10** | 规划漂移（多份路线图并存） | 🟠 中 | 决策混乱 | **本手册作为 SSOT**；其它文档路线图降级为指针 | 本手册 §0 |
| **R-11** | 20 章烂尾 | 🟠 中 | 项目失败 | M5 决策门 + 缩水预案（12~15 章） | 文档 2 §8 |
| **R-12** | 写作量爆炸（主线 4~6 万字 + 支援） | 🟡 中低 | 工期 | 支援先做 13 组核心；超支砍支援不动主线 | 文档 2 §8 |
| **R-13** | 上游框架频繁更新导致合并痛 | 🟡 中低 | 维护成本 | `framework.lock` 钉死；升级是显式动作 | 文档 6 §6.1 |
| **R-14** | 学习曲线陡（Proc/ARM/寄存器） | 🟡 中低 | 进度 | 先数据后代码最后汇编；框架文档齐全 | 文档 2 §8 |
| **R-15** | 关键文档缺失（关卡表从未创建） | 🟡 中低 | 关卡制作无输入 | M4 前产出或改用其它引用 | 本手册 §2.4 |
| **R-16** | **地图瓦片集自产能力未验证**（框架仅 Partially supported，"no tileset package format is accepted"） | 🟠 中高 | **阻塞全部 20 章地图** | **前移到 M3 攻坚**：优先复用 10 套原版瓦片集，仅局部新增；实机验证后再批量画图 | 文档 8 §14.5 |
| **R-17** | **同步脚本的槽位定位错位**：`characters.json` 256 条中 162 条无 `character` 键（只有 1-based 的 `characterId`），按 `c["character"]` 取键会 `KeyError`，按数组下标定位会**整体偏移一格** | 🔴 高 | **静默数据损坏**（角色数值写到隔壁槽位） | 合并前**显式断言 `idx + 1 == characterId`**，任一不符立即中止；禁止"猜"符号名对应槽位；每个整表 JSON 单独确认定位键 | 文档 6 §3.4 |
| **R-18** | **原创角色入表无现成通道**：框架对 11 张 generated-data 表强制 round-trip，而 `characters`/`classes`/`supports` **没有** `items` 那样的 `*_expansion.json` 豁免通道 | 🟢 低（**已定方案**） | —（原为"阻塞 M2 虞聪入表"） | ✅ **2026-09-25 调研完成，采用 B'（生成产物回填）**：JSON → `generate --no-roundtrip` → 产物覆盖 `src/data_characters.c`。**实测完整 `make` 通过、ROM 与手写改法逐字节相同（`b64d264a`）**。剩实施动作（写进 `shanhe-build.sh` + 反查预期清单） | 文档 6 §3.4b |

> **已撤销的风险**：中文字库容量（实测无压力，文档 2 §5.1）。
>
> **D-3 定案新增 R-16**（2026-09-25）：瓦片集是"1 次极高成本 + 不做则全盘阻塞"的一次性壁垒，原登记册未覆盖，现补录并前移到 M3。
>
> **M1 审计新增 R-17**（2026-09-25）：`characters.json` 的 256 条记录分两套互斥键方案（94 条 `character` 符号名 + 162 条 `characterId` 槽位号），是合并脚本最易踩的静默错位点。审计时实测 `c["character"]` 直接崩溃、`characterId == 下标 + 1`。
>
> **M1 实施新增 R-18**（2026-09-25）：实施 M1 时实测出框架的 round-trip 约束——16 张 generated-data 表中 11 张强行绑定手写 C 参考，改 JSON 不同步改手写 C 会让 `make` 硬失败（`generated_data.mk:685 Error 1`）。`items` 有官方豁免通道，`characters`/`classes`/`supports` 没有。**已调研定案 B' 方案**（生成产物回填），风险降级为 🟢。

---

## 8. 变更与决策管理

### 8.1 决策如何落档

| 类型 | 记在哪 |
|---|---|
| **项目级决策**（体量、路线、发布方式） | 本手册 §8.2 决策日志 + `2.设计文档.md` §1 |
| **技术决策**（架构、同步机制） | 对应专业文档（文档 6/7）|
| **数值决策** | `3.游戏数值设定.md`（唯一来源）|
| **未定事项** | 本手册 §10 待拍板总表 |

### 8.2 决策日志

| 日期 | 决策 | 结论 | 出处 |
|---|---|---|---|
| 2026-09-24 | 技术路线 | FE8 反汇编扩展框架（路线 A+） | 文档 0 §11 |
| 2026-09-25 | 版本管理 | 方案 B 内容外置 | 文档 2 §12.6 |
| 2026-09-25 | 资产章归属 | 独立为文档 8 | 文档 6 v1.4 |
| 2026-09-25 | 文档防篡改 | 流程防护 + `git-health.sh`（不设只读属性） | `GIT_WORKFLOW.md` |
| 2026-09-25 | 规划 SSOT | **本手册为唯一规划来源** | 本手册 §0 |
| 2026-09-25 | **D-2 神器特效** | 照搬原版神圣武器先例：物理神器走 banim 换色（零 ID）、法系神器走 `custom-spell-effect`；**9 件只需 3 个 ID**，16 个 ID 分配规划已定 | 文档 8 §7.2 |
| 2026-09-25 | **D-1 角色槽位分配** | 22 对 33 定案：含三处连锁优化（柴多福→雷诺克 / 白鹿鸣→罗斯 / 闻人夜→萨列）+ 柳青鸾改用 `CLASS_MYRMIDON_F` | 文档 9 §3.2/§3.4/§4.1 |
| 2026-09-25 | **D-3 美术预算重心** | 不做"立绘 vs 地图"二选一，改走**分期摊销**：地图瓦片集前置攻坚（M3）+ 立绘/地图按章交错摊开 | 文档 8 §14 |
| 2026-09-25 | **D-3 落地①：M3 增补** | M3 新增「地图瓦片集攻坚」为交付物 ⑤ / 验收标准 6（原风险 R-16 前移关闭） | 文档 8 §14.5/§14.7 |
| 2026-09-25 | **D-3 落地②：M4 修订** | M4 验收标准拆为「硬门禁 1~5」+「软指标 6~7」（7 套 Demo 立绘 + 单套立绘/单章地图工时记录），为 M5 决策门提供真数据 | 文档 8 §14.3/§14.7 |

### 8.3 范围变更流程

1. 提出变更 → 记入本手册 §8.2 决策日志（含理由）
2. 判断是否影响里程碑定义 → 若影响，同步更新 §4
3. 判断是否影响其它文档 → 同步更新并升版本号
4. **变更必须提交推送**（项目铁律）

---

## 9. 进度追踪机制

### 9.1 状态标记约定

| 标记 | 含义 |
|---|---|
| ✅ | 已完成，**门禁全过** |
| 🔵 | 进行中 |
| ⬜ | 未开始 |
| ⛔ | 被阻塞（注明阻塞项） |

### 9.2 进度看板

| 里程碑 | 状态 | 门禁通过项 | 备注 |
|---|---|---|---|
| M0 基线 | ✅ | 全部 | 2026-09-25 |
| M1 骨架 | ✅ | 全部 | 2026-09-25（提交 `26ecb08`/`bcb032c`/`9b5f7ea`） |
| M2 内容管线 | 🔵 | — | **当前**；原创角色通道已定案 B'（文档 6 §3.4b） |
| M3 机制外置 | ⬜ | — | |
| M4 Demo 五章 | ⬜ | — | |
| M5 决策门 | ⬜ | — | |
| M6 全量 | ⬜ | — | 体量待 M5 裁定 |
| M7 发布 | ⬜ | — | |

### 9.3 每次会话的收尾动作

1. 若推进了任务 → 更新 §9.2 看板
2. 若产生决策 → 记入 §8.2
3. 若发现新风险 → 记入 §7
4. **提交推送**（`GIT_WORKFLOW.md` 铁律）

---

## 10. 待拍板决策总表

**所有需要用户决定的事项集中在此**，各文档的"待决"章节均以此表为准。

| # | 决策 | 影响 | 最迟需要时机 | 相关文档 |
|---|---|---|---|---|
| D-1 | ~~**22 个原版角色槽位的具体分配**~~ | — | ✅ **已定案（2026-09-25）** | 文档 9 §3.2 矩阵 + §3.4 优化 |
| D-2 | ~~**9 件神器特效如何精简**~~ | — | ✅ **已定案（2026-09-25）**：9 件只需 3 个 ID | 文档 8 §7.2 |
| D-3 | ~~**美术预算重心**（立绘 vs 地图）~~ | — | ✅ **已定案（2026-09-25）**：走分期摊销（瓦片集前置 + 立绘/地图交错） | 文档 8 §14 |
| D-4 | **体量红线：20 章 vs 12~15 章** | 全量规模 | **M5 决策门** | 文档 2 §8 |
| D-5 | ~~**悔棋系统是否保留**~~ | — | ✅ **已定案（2026-09-25）：不做**。模拟器即时存档 + 原版中断存档兜底 | 文档 2 §2 |
| D-6 | ~~**关卡表的产出方式**（新建文档 / 并入 / 改引用）~~ | — | ✅ **已定案（2026-09-25）**：不建文档，直接数据化（每章一个 bundle） | `10.关卡表数据化方案.md` |
| D-7 | **双难度是否保留** | 数值与关卡工作量 | M6 前 | 文档 2 §1 |

> **已定案**：D-1（22 槽位分配）—— 含三处连锁优化与柳青鸾改用 `CLASS_MYRMIDON_F`，详见文档 9 §3.2/§3.4/§4.1。
> **已定案**：D-2（神器特效精简）—— 9 件只需 3 个 ID，详见文档 8 §7.2。
> **已定案**：D-3（美术预算重心）—— 分期摊销路线，详见文档 8 §14。
> **已定案**：D-5（悔棋系统）—— **不做**。理由：① 原版已有每回合中断存档；② 模拟器即时存档已是零成本兜底；③ 代码层快照回溯成本中等、收益低；④ **与"经典模式战死即永别"的硬派基调一致**。详见文档 2 §2。
> **已定案**：D-6（关卡表产出方式）—— 不建 Markdown 关卡表，设计意图留文档 1 §4.5/§4.6，数据落 `content/data/chN_*.json`，详见文档 10。
> **D-3 的 2 项落地确认已于 2026-09-25 定案**：① M3 新增「地图瓦片集攻坚」为交付物 ⑤ / 验收标准 6；② M4 验收标准修订——保留"可用原版"为硬门禁第 3 条，新增软指标 6/7（7 套 Demo 立绘 + 工时记录）。
> **✅ 已无"可立即决定"的待拍板项。** D-4（体量红线）须等 M5 决策门（依赖 M4 的立绘/地图工时实测数据），D-7（双难度）最迟 M6 前。**M1 已于 2026-09-25 完成，下一步推进 M2（开篇先做原创角色入表通道专项调研）。**

---

## 附录 A：与其它文档的分工

| 文档 | 职责 | 与本手册的关系 |
|---|---|---|
| `0.调研报告.md` | 技术选型论证 | 提供决策依据 |
| `1.剧情与世界观设定.md` | 故事 | 提供内容输入 |
| `2.设计文档.md` | 系统设计意图 | **其 §7 路线图降级为指针** → 本手册 §4 |
| `3.游戏数值设定.md` | 所有数字 | **其 §13 TODO 作为本手册 S6 的明细** |
| `4.项目规划手册.md`（本文） | **规划 SSOT** | — |
| `5.改版工程手册.md` | 工程操作 | **其 §8 待办并入本手册 §2.3** |
| `6.方案B内容外置规划.md` | 方案 B 细节 | **其 §4 路线图降级为指针** → 本手册 §4 |
| `7.框架架构与机制详解.md` | 框架说明书 | 提供技术约束 |
| `8.原创资产体系与框架资产对照.md` | 资产格式与登记 | 提供 S5 的规格 |
| `9.角色槽位映射与美术平替方案.md` | 槽位映射 | 提供 D-1 的选项 |
| `10.关卡表数据化方案.md` | 关卡数据的产出方式（D-6） | 提供 M4 关卡制作的输入规格 |

> **编号说明**：`4.关卡表_Demo五章.md` 曾被 5 处引用但**从未创建**（详见 §2.4 第 6 项、文档 10）。2026-09-25 本手册占用 4 号后，那 5 处引用已统一改指本手册；**其产出方式（D-6）已于 2026-09-25 定案为「不建文档、直接数据化」，落点 `10.关卡表数据化方案.md`**。至此 `docs/` 编号连续：**0~10**。

## 附录 B：术语表

| 术语 | 含义 |
|---|---|
| **方案 B** | 内容外置：原创内容放本仓库 `content/`，框架降为只读依赖 |
| **拥有缝（owning seam）** | 框架中负责登记某类资产的唯一入口 |
| **四要素** | 资产进游戏的四个必要条件：源文件包 / 确定性转换 / 完整登记 / 运行时消费者 |
| **门禁（Gate）** | 里程碑的通过条件，全项为真才算完成 |
| **三动词** | `validate / generate / check` 数据管线 |
| **双环克制** | 兵刃三环（枪刀锤）+ 三才法环（天地人） |
| **决策门** | M5，基于 Demo 速度裁定项目体量 |

## 附录 C：版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| **v1.9** | **2026-09-25** | **M2 首批：B' 回填落地**。① M2「注意」更新——B' 已实现于 `shanhe-build.sh` 3a'（三道：数据校验 → B' 回填 → round-trip 复核）；② 用虞聪数值顶替艾莉卡槽位实测通过（ROM `95b14071`），产物数值与文档 3 §4.2 逐项一致；③ 附带修复第 6 步反查的 `read` IFS bug（首次暴露，曾致所有改动被误报「预期外」）。详见文档 11 §10 |
| **v1.8** | **2026-09-25** | **M2 前置调研完成（原创角色入表通道）**：四条出路全部实测，定案 **B'（生成产物回填）**——A（items 式豁免）❌ 框架无此机制，B（双写）✅、B'（回填）✅ 推荐（全自动、ROM 与 B 逐字节相同 `b64d264a`），C（运行时改写）⚠️ 未测，**D（移走手写 C）❌ 实测否决**（被本地化管线读作输入）。① §7 R-18 降级 🟢 并记录方案；② M1「关键发现」与 M2「注意」同步 B' 结论；③ §9.2 看板 M2 备注更新。详见文档 6 §3.4b |
| **v1.7** | **2026-09-25** | **M1 完成，切换到 M2**：① M1 标题标记 ✅ 已完成，六条验收逐条勾选；② 新增「M1 完成时的关键发现（交接给 M2）」——round-trip 约束（详见文档 6 §3.4a）；③ M2 标题标记 ← 当前目标，注意事项补入「开篇先做原创角色入表通道专项调研」；④ §9.2 看板 M1 → ✅（提交 `26ecb08`/`bcb032c`/`9b5f7ea`）、M2 → 🔵 当前；⑤ §10 结论更新为「M1 已完成，下一步 M2」 |
| **v1.6** | **2026-09-25** | **M1 骨架全量审计修正**：① M1 验收标准第 2 条「与基线行为一致（boot-check + 目视）」**替换为可执行的产物校验**（尺寸 + header + 实机可引导 + 目视中文）——审计发现官方 `expansion-modern-boot-check` 用逐帧像素哈希比对，改中文后**必然失败**，不可作验收工具；② M1 风险行补「验收工具的选择」并加注说明；③ 新增 §7 风险册 **R-17 同步脚本的槽位定位错位风险**（见文档 6 §3.4 纠错） |
| v1.5 | 2026-09-25 | **D-5 定案（悔棋不做）**：① §10 D-5 标记已定案（指针 → 文档 2 §2）并补入 4 条理由；② §2.4 第 5 项关闭，阻塞项**全部清零**；③ §2.2 M1 并行推进项改为「**已无待决项，直接推进 M1**」；④ §4 M5 判定内容去掉"悔棋"；⑤ 结论更新为「唯一剩余 D-4，属 M5 决策门裁定项，不可提前决定」 |
| v1.4 | 2026-09-25 | **D-6 定案**：① §10 D-6 标记已定案（不建 Markdown 关卡表，直接数据化，指针 → 文档 10）；② §2.4 第 6 项关闭；③ §2.2 M1 并行推进项由 D-6 改为 **D-5**；④ 附录 A 文档分工表新增 `10.关卡表数据化方案.md`，编号说明改为 `0~10`；⑤ 同步文档 2 的两处引用 |
| v1.3 | 2026-09-25 | **D-3 落地两项定案**：① M3 标题改为「机制外置 + 美术前置攻坚」，新增交付物 ⑤（地图瓦片集攻坚）与验收标准 6；② M4 验收标准拆为「硬门禁 1~5」+「软指标 6~7」（7 套 Demo 立绘 + 单套立绘/单章地图工时记录）；③ M5 判定依据补入工时样本；④ §2.3 S3/S5 工作流补录；⑤ M1 并行推进项收敛为 D-6。D-1/D-2/D-3 三项资产决策全部定案 |
| v1.2 | 2026-09-25 | **D-3 定案**：§10 D-3 标记已定案（分期摊销，指针 → 文档 8 §14）；§7 风险册新增 **R-16 地图瓦片集自产能力未验证**（🟠 中高）；§2.4 新增第 7/8 项待确认 |
| v1.1 | 2026-09-25 | **D-1/D-2 定案**：§10 D-1 标记已定案（指针 → 文档 9 §3.2/§3.4）、D-2 标记已定案（9 件神器只需 3 个 ID，指针 → 文档 8 §7.2）；§2.4 第 1/2 项标记已定案 |
| v1.0 | 2026-09-25 | 首版。整合文档 2 §7 与文档 6 §4 两套并存路线图，建立规划 SSOT；含项目定义、当前基线、七条工作流 WBS、M0~M7 里程碑与门禁、关键路径、15 项风险登记、变更与进度机制、7 项待拍板决策 |
